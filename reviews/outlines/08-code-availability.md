# Outline: Code availability

- **Goal**: Enumerate code artifacts required to reproduce the dataset.
- **Key points**:
  - Tethys (`pip install tethys-downscale`) + GitHub.
  - Integration meta-repo (run drivers, preprocessing, postprocess, validation).
  - Demeter, CERF, TGW-WRF.
  - Reference to `PIPELINE.md` for end-to-end reproduction.
